
<% @projects.each do |project| %>
    <p><%= link_to "#{project.name}", project_path(project.id) %> by <%= link_to "#{project.owner.name}", projects_started_by_path(project.owner_id) %></p>
<% end %>
<hr>
<p><%= link_to "View All Projects", projects_path %></p>
<p><%= link_to "Start A New Project", new_project_path %></p>