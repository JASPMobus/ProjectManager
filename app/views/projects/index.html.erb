
<% @projects.each do |project| %>
    <p><%= link_to "#{project.name} by #{project.owner.name}", project_path(project.id) %></p>
<% end %>
<hr>
<p><%= link_to "Start A New Project", new_project_path %></p>